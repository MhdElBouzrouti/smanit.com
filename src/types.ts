export type Language = 'fr' | 'en';

export interface Translations {
  tag: string;
  tagline: string;
  subtext: string;
  notifyTitle: string;
  placeholder: string;
  button: string;
  submitting: string;
  successMessage: string;
  contactPrompt: string;
  contactEmail: string;
  footerRights: string;
  status: string;
}
